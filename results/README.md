# results/

Experiment outputs (tables, figures, sweep summaries). Payloads are
gitignored; summary markdown reports are tracked. Each experiment writes to
`results/eNN_<name>/` and links its summary from the experiment README.
